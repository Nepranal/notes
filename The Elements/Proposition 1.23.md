# Proposition

> To construct a rectilinear angle equal to a given rectilinear angle at a (given) point on a given straight-line.

# Method

![alt text](<../sources/The Elements/Screenshot 2025-12-29 at 9.45.16 AM.png>)

Construct $ECD$ at $F$.

The idea is to build a triangle that matches exactly $\triangle CDE$. Proposition 1.22 allows us and so what's left is to make sure $AF = CD, FG = CE$ or vice versa and that $AG = DE$. Since now all lengths match, the angles must also match.
